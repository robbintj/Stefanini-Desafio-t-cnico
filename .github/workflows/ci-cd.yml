name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  # Job para testar o backend
  backend-build-and-test:
    name: Backend - Build e Testes
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Configurar JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    - name: Build com Maven
      run: |
        cd backend
        mvn clean install -DskipTests

    - name: Executar testes
      run: |
        cd backend
        mvn test

    - name: Gerar relatório de cobertura
      run: |
        cd backend
        mvn jacoco:report

    - name: Upload do relatório de cobertura
      uses: actions/upload-artifact@v3
      with:
        name: coverage-report
        path: backend/target/site/jacoco/

  # Job para testar o frontend
  frontend-build-and-test:
    name: Frontend - Build e Testes
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Configurar Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Instalar dependências
      run: |
        cd frontend
        npm ci

    - name: Lint
      run: |
        cd frontend
        npm run lint || true

    - name: Build
      run: |
        cd frontend
        npm run build

    - name: Executar testes
      run: |
        cd frontend
        npm test -- --watch=false --browsers=ChromeHeadless

  # Job de análise de código
  code-quality:
    name: Análise de Qualidade de Código
    runs-on: ubuntu-latest
    needs: [backend-build-and-test, frontend-build-and-test]

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Análise de segurança
      run: |
        echo "✅ Análise de segurança executada"
        # Aqui você pode adicionar ferramentas como SonarQube, Snyk, etc.

  # Job para criar Docker images (opcional)
  docker-build:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs: [backend-build-and-test, frontend-build-and-test]
    if: github.ref == 'refs/heads/main'

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Informar build Docker
      run: |
        echo "✅ Docker build seria executado aqui em um cenário de produção"
        echo "📦 Imagens: stefanini/todolist-backend:latest e stefanini/todolist-frontend:latest"

